import * as React from 'react';
import styled from "styled-components";

// Delete me
const Thing = () => {
  return <StyledDiv>the snozzberries taste like snozzberries</StyledDiv>;
};

const StyledDiv = styled.div`
  color: pink;
`

export default Thing;
